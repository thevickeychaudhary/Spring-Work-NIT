spring.application.name=BootMS-Proj01-RAndServer-Eureka

# Server port
server.port=8761

# Eureka Server Configuration (correct property names)
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

# Optional: Disable self-preservation for development
eureka.server.enable-self-preservation=false